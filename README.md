# final-exam_devops

**Note:** I used Docker Compose to run MySQL in a container. The Spring Boot application connects to the containerized MySQL database through localhost:3307. The database name is idcard_db, and Hibernate automatically creates the required tables from the JPA entities.